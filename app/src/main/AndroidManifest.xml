<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">

        <!-- SplashActivity será a primeira tela -->
        <activity
            android:name=".ui.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login -->
        <activity
            android:name=".ui.LoginActivity"
            android:exported="true" />

        <!-- Telas do Administrador -->
        <activity android:name=".ui.administrador.AdministradorActivity" android:exported="true" />
        <activity android:name=".ui.administrador.AdministradorDisciplinasActivity" android:exported="true" />
        <activity android:name=".ui.administrador.AdministradorTurmasActivity" android:exported="true" />
        <activity android:name=".ui.administrador.AdministradorProfessoresActivity" android:exported="true" />
        <activity android:name=".ui.administrador.AdministradorAlunosActivity" android:exported="true" />

        <!-- Telas do Professor -->
        <activity android:name=".ui.professor.ProfessorActivity" android:exported="true" />
        <activity android:name=".ui.professor.ProfessorTurmasActivity" android:exported="true" />
        <activity android:name=".ui.professor.ProfessorDisciplinasActivity" android:exported="true" />

        <!-- Telas do Aluno -->
        <activity android:name=".ui.aluno.AlunoActivity" android:exported="true" />
        <activity android:name=".ui.aluno.DisciplinasAlunoActivity" android:exported="true" />
        <activity android:name=".ui.aluno.TurmasAlunoActivity" android:exported="true" />

    </application>
</manifest>
